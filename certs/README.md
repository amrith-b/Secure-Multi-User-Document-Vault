This directory is intended for TLS certificates and keys used for local testing.

Do NOT commit real certificates or private keys to GitHub.

Example expected files (for local setup):
- private_key.key
- public_key.pub
- CA.crt
